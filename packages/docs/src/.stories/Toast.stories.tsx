import type { Meta, StoryObj } from '@storybook/react'
import {  Toast } from '@samuel-ignite-ui/react'

export default {
  title: 'Feedback/Toast',
  component: Toast,
} as Meta

export const Primary: StoryObj = {}